อยากใช้ Llama Guard:

Bash
docker-compose -f docker-compose-lama.yml up --build
อยากใช้ NeMo (Memo):

Bash
docker-compose -f docker-compose-memo.yml up --build
อยากใช้ Guardrails AI:

Bash
docker-compose -f docker-compose-gai.yml up --build