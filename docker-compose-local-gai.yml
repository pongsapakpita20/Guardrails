version: '3.8'

services:
  # 🟢 1. ดึง Ollama กลับมา (Service นี้จะรัน Ollama ใน Docker)
  ollama:
    image: ollama/ollama:latest
    container_name: guardrails-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_storage:/root/.ollama
    networks:
      - ai-net

  # 🟠 2. Backend (ยิง API หา Service ข้างบน)
  backend:
    build:
      context: ./backend
      args:
        GUARDRAILS_TOKEN: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..DOijp5oC0pZZFhmrRkXGw00GlNV3dnWfXOgbLtBIofs
    container_name: guardrails-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/src:/app/src
    environment:
      # 👇 จุดสำคัญ: ยิงไปหา Service ชื่อ "ollama" ผ่าน Docker Network
      - OLLAMA_URL=http://ollama:11434
      - GUARD_ENGINE=guardrails_ai
    depends_on:
      - ollama
    networks:
      - ai-net

  # 🔵 3. Frontend (เหมือนเดิม)
  frontend:
    build:
      context: ./frontend
    container_name: guardrails-ui
    ports:
      - "5173:5173"
    volumes:
      - ./frontend/src:/app/src
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:8000
    depends_on:
      - backend
    networks:
      - ai-net

volumes:
  ollama_storage:


networks:
  ai-net:
    driver: bridge
